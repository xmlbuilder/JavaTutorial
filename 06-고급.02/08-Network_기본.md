# 📘 6. 네트워크 - 기본 이론 요약
## 1. 클라이언트와 서버
- 클라이언트: 서비스를 요청하는 주체 (예: 손님, 고객, 사용자)
- 서버: 요청을 받아 서비스를 제공하는 주체 (예: 주방, 은행, 미용사)
- 클라이언트-서버 모델: 요청과 응답의 구조로 구성된 서비스 관계

- 객체지향 관점 예시
    - Main 객체 → Service 객체에게 메서드 호출 → 요청
    - Service 객체 → 요청 수행 → 응답
    - 반환값이 없어도 요청을 수행하면 서버 역할

- 중첩된 관계 예시
    - Main → Service → Repository 구조에서:
    - Main은 클라이언트
    - Service는 서버이자 클라이언트
    - Repository는 서버

## 2. 네트워크 기본 개념
- 네트워크: 여러 컴퓨터가 연결되어 데이터를 주고받는 환경
- 인터넷: 대표적인 네트워크, 클라이언트-서버 모델 기반
- 예: 스마트폰 → 웹사이트 요청 → 서버가 응답

## 3. 인터넷 통신 구성 요소
- 📡 IP (Internet Protocol)
    - IP 주소를 기반으로 데이터 전달
    - 데이터는 패킷 단위로 전송됨
    - 패킷 구조: 출발지 IP, 목적지 IP, 데이터
-⚠️ IP의 한계
    - 비연결성: 대상이 없어도 패킷 전송
    - 비신뢰성: 패킷 손실, 순서 뒤바뀜 가능
    - 프로그램 구분 어려움: 동일 IP 내 여러 애플리케이션

## 4. TCP와 UDP
-🔒 TCP (Transmission Control Protocol)  
    - 연결지향: 3-way handshake
    - 신뢰성 보장: 데이터 전달 확인, 순서 보장
    - 패킷 재전송: 순서 오류 시 재요청  
- TCP 3-way handshake
    - SYN (연결 요청)
    - SYN+ACK (응답)
    - ACK (확인)
    - 데이터 전송
- ⚡ UDP (User Datagram Protocol)
    - 비연결성, 비신뢰성
    - 빠르지만 순서 보장 없음
    - 애플리케이션에서 보완 필요

## 5. PORT
- 포트 번호: 하나의 IP에서 여러 프로그램 구분
- 범위: 0 ~ 65535
- 0 ~ 1023: 잘 알려진 포트 (예: HTTP 80, HTTPS 443)
- 예: 게임 서버(8090), 화상통화(21000), 웹 브라우저(10010)

## 6. DNS (Domain Name System)
- 도메인명 → IP 주소 변환
- 전화번호부처럼 작동
- 예: google.com → 200.200.200.2
- DNS 사용 흐름
    - 클라이언트가 도메인명 요청
    - DNS 서버가 IP 주소 응답
    - 클라이언트가 해당 IP로 접속

## 7. 인터넷 네트워크 정리
- 핵심 구성 요소:
- IP
- TCP / UDP
- PORT
- DNS

---
