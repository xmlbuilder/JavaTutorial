# HTTP - 기본 이론

문서는 HTTP의 핵심 개념과 구조를 단계별로 설명하며, 웹 서버 개발을 위한 기초 지식을 제공합니다.  
아래에 내용을 단계별 요약, 핵심 구조, 그리고 실제 메시지 예시 중심으로 정리.

# 🧭 전체 흐름 요약
- 1. HTTP란 무엇인가?
- 2. 클라이언트-서버 구조
- 3. HTTP 메시지 구조
- 4. 요청 메시지 구성
- 5. 응답 메시지 구성
- 6. 주요 HTTP 메서드 (GET, POST)



## 🌐 1. HTTP란?
- 웹 브라우저와 서버 간 통신을 위한 표준 프로토콜
- 모든 데이터 전송은 HTTP 메시지로 이루어짐
- 전송 가능한 데이터:
- HTML, TEXT, 이미지, 영상, JSON, XML, 파일 등

## 🔁 2. 클라이언트-서버 구조
- 요청(Request): 클라이언트가 서버에 요청
- 응답(Response): 서버가 결과를 만들어 클라이언트에 응답
```
[클라이언트] → HTTP 요청 → [서버]
[서버] → HTTP 응답 → [클라이언트]
```


## ✉️ 3. HTTP 메시지 구조
```
start-line
headers
(empty line)
message body
```
### ✅ 예시 요청 메시지 (GET)
```
GET /search?q=hello&hl=ko HTTP/1.1
Host: www.google.com
```

### ✅ 예시: 응답 메시지
```
HTTP/1.1 200 OK
Content-Type: text/html;charset=UTF-8
Content-Length: 3423

<html>
<body>Hello World</body>
</html>
```


## 📬 4. 요청 메시지 구성
| 구성 요소       | 예시                                | 설명                                      |
|----------------|-------------------------------------|-------------------------------------------|
| 메서드         | `GET`                               | 서버에게 어떤 동작을 요청할지 지정        |
| 요청 대상      | `/search?q=hello&hl=ko`             | 요청할 리소스 경로 및 쿼리 파라미터       |
| HTTP 버전      | `HTTP/1.1`                           | 사용 중인 HTTP 프로토콜 버전              |
| Host 헤더      | `Host: www.google.com`              | 요청 대상 서버의 도메인 정보              |
| 기타 헤더      | `User-Agent`, `Accept`, `Cookie` 등 | 요청에 필요한 부가 정보                   |
| 메시지 바디    | (GET은 없음, POST는 있음)           | 요청 데이터 (POST, PUT 등에서 사용)       |

### ✅ 예시 요청 메시지 (GET)
```
GET /search?q=hello&hl=ko HTTP/1.1
Host: www.google.com
```

## 📤 5. 응답 메시지 구성
| 구성 요소       | 예시                                  | 설명                                      |
|----------------|---------------------------------------|-------------------------------------------|
| 상태 라인       | `HTTP/1.1 200 OK`                     | HTTP 버전 + 상태 코드 + 이유 문구         |
| 헤더            | `Content-Type: text/html;charset=UTF-8`<br>`Content-Length: 3423` | 응답 데이터에 대한 부가 정보              |
| 공백 라인       | (CRLF)                                | 헤더와 바디를 구분하는 빈 줄              |
| 메시지 바디     | `<html><body>Hello World</body></html>` | 실제 응답 데이터 (HTML, JSON, 이미지 등)  |

### ✅ 예시: 응답 메시지
```
HTTP/1.1 200 OK
Content-Type: text/html;charset=UTF-8
Content-Length: 3423

<html>
<body>Hello World</body>
</html>
```

## 🛠️ 6. 주요 HTTP 메서드
### ✅ GET
- 리소스 조회
- 데이터는 URL 쿼리로 전달
- 메시지 바디는 사용하지 않음
```
GET /search?q=hello&hl=ko HTTP/1.1
Host: www.google.com
```

## 📨 POST
- 데이터 등록, 처리
- 메시지 바디에 데이터 포함
```
POST /add-member HTTP/1.1
Content-Type: application/x-www-form-urlencoded
Content-Length: 22

id=id1&name=name1&age=20
```

## 📌 핵심 개념 요약
| 항목             | 설명                                                                 |
|------------------|----------------------------------------------------------------------|
| HTTP 메시지       | 요청과 응답 모두 메시지 형식으로 구성됨                             |
| 메서드 종류       | GET, POST, PUT, DELETE, PATCH 등                                     |
| 상태 코드         | 200 OK, 404 Not Found, 500 Internal Server Error 등                  |
| 헤더 역할         | 전송 정보, 인증, 캐시, 클라이언트 정보 등 포함 가능                 |
| 메시지 바디       | 실제 전송할 데이터 (HTML, JSON, 이미지 등)                          |
| 클라이언트-서버 구조 | 요청 → 응답 흐름으로 동작, 서버는 요청을 처리해 응답 생성             |

---

