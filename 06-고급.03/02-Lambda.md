# 람다
 **람다** 의 전체 내용을 빠짐없이 정리한 구조화 요약입니다.  
 이 문서는 자바의 람다 표현식을 중심으로 함수형 인터페이스, 문법 생략, 전달 및 반환,  
 고차 함수까지 단계적으로 설명하고 있습니다.

## 📘 문서 개요: 람다
| 단계                     | 설명                                                                 | 핵심 키워드                         |
|--------------------------|----------------------------------------------------------------------|-------------------------------------|
| 1️⃣ 람다 정의              | 자바 8부터 도입된 익명 함수 문법. `(매개변수) -> {본문}` 형식 사용     | 익명 함수, 람다식, 문법 구조         |
| 2️⃣ 함수형 인터페이스      | 단일 추상 메서드(SAM)를 가진 인터페이스. 람다의 대상이 되는 타입       | @FunctionalInterface, SAM, 시그니처 |
| 3️⃣ 람다 문법 생략         | 타입 추론, 중괄호 생략, return 생략, 매개변수 괄호 생략 등 문법 축약    | 타입 생략, 단일 표현식, 간결성       |
| 4️⃣ 람다 전달 및 반환      | 람다를 변수에 저장, 메서드에 전달, 메서드에서 반환 가능                | 참조값 전달, 고차 함수, 유연성       |
| 5️⃣ 고차 함수 개념         | 함수를 인자로 받거나 반환하는 함수. 람다를 값처럼 다루는 고급 추상화    | 고차 함수, 함수 전달, 함수 반환      |
| 6️⃣ 실전 문제 풀이         | 중복 제거, 값 매개변수화, 코드 조각 전달 등 람다 활용 리팩토링 실습     | 리팩토링, 재사용성, 함수 분리        |


## 1️⃣ 람다 정의
- 자바 8부터 도입된 익명 함수 문법
- 기본 형식: (매개변수) -> {본문}
- 일반 메서드와 비교:
```java
public int add(int x) { return x + 1; } // 일반 메서드

(int x) -> { return x + 1; }            // 람다식
```
- 람다는 클래스 없이도 함수처럼 표현 가능하지만, 실제로는 클래스 인스턴스로 생성됨

## 2️⃣ 람다의 특징
- 간결성: 익명 클래스보다 코드가 짧고 명확
- 변수처럼 사용 가능:

```java
Procedure p = () -> { System.out.println("hello"); };
p.run();
```

- 실행 시 클래스 인스턴스 생성됨:
- 익명 클래스: `InstanceMain$1`
- 람다: `$$Lambda/...`

## 3️⃣ 함수형 인터페이스
- 정의: 추상 메서드가 하나만 있는 인터페이스
- 예시:
```java
@FunctionalInterface
public interface Procedure {
    void run();
}
```
- 제한: 추상 메서드가 2개 이상이면 람다 할당 불가
- 보장: @FunctionalInterface 애노테이션으로 컴파일 시 검증

## 4️⃣ 람다와 시그니처
- 람다는 함수형 인터페이스의 메서드 시그니처와 일치해야 함
- 시그니처 구성:
- 매개변수 수와 타입
- 반환 타입
- 예시:
```java
MyFunction f = (int a, int b) -> a + b;
```
## 5️⃣ 람다의 문법 생략
### ✅ 단일 표현식 생략
- 중괄호 {}와 return 생략 가능
```java
(int a, int b) -> a + b;
```

### ✅ 타입 추론
- 컴파일러가 타입 자동 추론
```java
MyFunction f = (a, b) -> a + b;
```

### ✅ 매개변수 괄호 생략
- 매개변수가 하나일 경우 괄호 생략 가능
```java
MyCall c = value -> value * 2;
```


## 6️⃣ 람다의 전달 방식
### 📌 변수에 대입
```java
MyFunction add = (a, b) -> a + b;
MyFunction cal = add;
```

### 📌 메서드에 전달
```java
calculate((a, b) -> a + b);
```

### 📌 메서드에서 반환
```java
static MyFunction getOperation(String op) {
    return (a, b) -> a + b;
}
```


## 7️⃣ 고차 함수 (Higher-Order Function)
- 정의: 함수를 인자로 받거나 반환하는 함수
- 자바에서는 함수형 인터페이스를 통해 람다를 전달/반환
- 예시:
```java
static void calculate(MyFunction f) { ... }
static MyFunction getOperation(String op) { ... }
```

## 8️⃣ 문제와 풀이 예제
### 🧩 문제 1: 중복 메시지 출력 리팩토링
- 기존:
```java
public static void greetMorning() {
    System.out.println("===시작===");
    System.out.println("Good Morning!");
    System.out.println("===끝===");
}
```
- 리팩토링:
```java
public static void greet(String message) {
    System.out.println("===시작===");
    System.out.println(message);
    System.out.println("===끝===");
}
```


### 🧩 문제 2: 값 매개변수화
- 숫자와 단위를 분리하여 재사용 가능한 출력 메서드로 개선

## ✅ 핵심 요약

| 항목                     | 설명                                                                 |
|--------------------------|----------------------------------------------------------------------|
| 람다 정의                | 자바 8부터 도입된 익명 함수 문법. `(매개변수) -> {본문}` 형식 사용       |
| 함수형 인터페이스        | 단 하나의 추상 메서드(SAM)를 가진 인터페이스. 람다의 대상이 되는 타입     |
| @FunctionalInterface     | 함수형 인터페이스임을 명시하는 애노테이션. 컴파일 시 규칙 위반을 방지     |
| 시그니처 일치            | 람다의 매개변수와 반환 타입은 인터페이스의 메서드 시그니처와 일치해야 함   |
| 문법 생략                | 단일 표현식일 경우 `{}`와 `return` 생략 가능. 타입 추론 및 괄호 생략 가능 |
| 전달 및 반환             | 람다를 변수에 저장하거나 메서드에 전달, 반환 가능. 참조값으로 처리됨       |
| 고차 함수                | 함수를 인자로 받거나 반환하는 함수. 람다를 값처럼 다루는 고급 추상화 방식 |

---


