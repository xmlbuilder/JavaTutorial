# 람다가 필요한 이유
 내용을 핵심 주제별로 정리한 요약입니다.  
 이 문서는 람다 표현식이 왜 필요한지를 단계적으로 설명하며,  
 코드 리팩토링 과정을 통해 함수형 프로그래밍의 필요성을 자연스럽게 이끌어냅니다.

## 📘 문서 개요: 람다가 필요한 이유

| 단계                     | 설명                                                             | 핵심 키워드                     |
|--------------------------|------------------------------------------------------------------|---------------------------------|
| 1️⃣ 값 매개변수화          | 변하지 않는 부분과 변하는 문자열을 분리하여 재사용성 향상         | 중복 제거, 문자열 파라미터화    |
| 2️⃣ 코드 조각 전달         | 코드 블록을 인터페이스로 추상화하여 메서드에 전달                 | Procedure 인터페이스, 실행 시간 |
| 3️⃣ 함수형 인터페이스 도입 | 코드 조각을 객체로 전달 → 람다 표현식으로 발전                    | 동작 매개변수화, 람다의 필요성  |


## 1️⃣ 람다가 필요한 이유 1: 값 매개변수화
### 🔍 문제 상황
```java
public static void helloJava() {
    System.out.println("프로그램 시작");
    System.out.println("Hello Java");
    System.out.println("프로그램 종료");
}
```
- helloJava(), helloSpring() 등 유사한 구조의 중복 코드 존재

### ✅ 리팩토링
```java
public static void hello(String str) {
    System.out.println("프로그램 시작");
    System.out.println(str); // 변하는 부분
    System.out.println("프로그램 종료");
}
```

- 핵심 개념: 변하지 않는 부분은 고정, 변하는 부분은 매개변수로 전달
- 기법: 값 매개변수화 (Value Parameterization)

## 2️⃣ 람다가 필요한 이유 2: 코드 조각 전달
### 🔍 문제 상황
```java
public static void helloDice() {
    long start = System.nanoTime();
    int value = new Random().nextInt(6) + 1;
    System.out.println("주사위 = " + value);
    long end = System.nanoTime();
    System.out.println("실행 시간: " + (end - start));
}
```
- helloDice(), helloSum() 등에서 코드 조각만 다르고 나머지는 동일
###  ✅ 리팩토링
#### 1. 함수형 인터페이스 정의
```java
package lambda;
public interface Procedure {
    void run();
}
```

### 2. 공통 메서드로 추출
```java
public static void hello(Procedure procedure) {
    long start = System.nanoTime();
    procedure.run(); // 코드 조각 실행
    long end = System.nanoTime();
    System.out.println("실행 시간: " + (end - start));
}
```

### 3. 코드 조각 전달
```java
static class Dice implements Procedure {
    public void run() {
        int value = new Random().nextInt(6) + 1;
        System.out.println("주사위 = " + value);
    }
}
```
```java
static class Sum implements Procedure {
    public void run() {
        for (int i = 1; i <= 3; i++) {
            System.out.println("i = " + i);
        }
    }
}
```

- 핵심 개념: 코드 조각을 객체로 전달
- 기법: 동작 매개변수화 (Behavior Parameterization)

## 🧠 핵심 개념 요약

| 개념                     | 설명                                                                 |
|--------------------------|----------------------------------------------------------------------|
| 값 매개변수화             | 변하는 값을 메서드의 인자로 전달하여 중복 제거 및 재사용성 향상         |
| 동작 매개변수화           | 변하는 "코드 조각"을 인터페이스로 추상화하여 메서드에 전달               |
| 함수형 인터페이스         | 단 하나의 추상 메서드를 가진 인터페이스. 람다 표현식의 대상이 됨         |
| `@FunctionalInterface`   | 함수형 인터페이스임을 명시하는 애노테이션. 컴파일러가 규칙을 검증함       |
| 람다 표현식               | 코드 조각을 간결하게 표현하는 문법. 익명 클래스보다 짧고 명확함           |


- 이후 단계에서는 이 구조를 기반으로 익명 클래스 → 람다 표현식 → 메서드 참조로 발전해 나가며,  
    자바의 함수형 프로그래밍 스타일을 본격적으로 학습하게 됩니다.
