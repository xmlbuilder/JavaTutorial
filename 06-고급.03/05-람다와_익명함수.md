## 🧠 람다 vs 익명 클래스 핵심 비교

| 항목             | 익명 클래스                          | 람다 표현식                         |
|------------------|--------------------------------------|-------------------------------------|
| 문법             | `new 인터페이스() { ... }`           | `매개변수 -> 실행문`                |
| 코드 간결성      | 장황하고 반복적                      | 간결하고 핵심만 표현 가능           |
| 구현 대상        | 여러 메서드 인터페이스도 구현 가능   | 함수형 인터페이스만 구현 가능       |
| this 키워드      | 익명 클래스 자신을 가리킴            | 외부 클래스 인스턴스를 가리킴       |
| 상태 관리        | 내부 필드, 멤버 변수 사용 가능       | 상태 없음 (함수로 동작)             |
| 캡처링           | final 또는 사실상 final만 접근 가능  | 동일하게 final 또는 사실상 final만 접근 가능 |
| 생성 방식        | `OuterClass$1.class` 생성됨          | `invokeDynamic`으로 동적 처리       |
| 호환성           | 자바 모든 버전에서 사용 가능         | 자바 8 이상에서만 사용 가능         |


## 📌 예제 요약
### ✅ 익명 클래스 예시
```java
button.setOnClickListener(new OnClickListener() {
    @Override
    public void onClick(View v) {
        System.out.println("버튼 클릭");
    }
});
```

### ✅ 람다 표현식 예시
```java
button.setOnClickListener(v -> System.out.println("버튼 클릭"));
```

### 🔍 this 키워드 차이
- `익명 클래스`: this → 익명 클래스 인스턴스
- `람다`: this → 외부 클래스 인스턴스
```java
Runnable lambda = () -> {
    System.out.println("람다 this: " + this); // 외부 클래스 참조
};
```

### 🔐 캡처링 예시
```java
final int a = 10;
int b = 20; // 사실상 final
int c = 30; // 변경됨

Runnable r = () -> {
    System.out.println(a); // OK
    System.out.println(b); // OK
    // System.out.println(c); // 컴파일 오류
};
```


## ✅ 언제 무엇을 써야 할까?

| 상황 또는 목적                         | 익명 클래스 사용이 적합한 경우             | 람다 표현식 사용이 적합한 경우             |
|----------------------------------------|--------------------------------------------|--------------------------------------------|
| 상태 유지가 필요한 경우                | ✅ 내부 필드로 상태 관리 가능               | ❌ 상태를 가질 수 없음                      |
| 여러 메서드를 구현해야 하는 경우       | ✅ 다중 메서드 인터페이스 구현 가능        | ❌ 단일 메서드만 구현 가능                  |
| 복잡한 인터페이스 구현이 필요한 경우  | ✅ 다양한 메서드 오버라이드 가능           | ❌ 함수형 인터페이스만 구현 가능            |
| 코드 간결성과 가독성이 중요한 경우    | ❌ 코드가 장황하고 반복적                  | ✅ 간결하고 핵심만 표현 가능                |
| 자바 8 이전 버전 호환이 필요한 경우    | ✅ 모든 자바 버전에서 사용 가능            | ❌ 자바 8 이상에서만 사용 가능             |
| 외부 클래스의 컨텍스트를 유지해야 할 때| ❌ this는 익명 클래스 자신을 가리킴        | ✅ this는 외부 클래스 인스턴스를 가리킴     |
| 성능과 메모리 효율이 중요한 경우       | ❌ 클래스 파일 생성으로 오버헤드 발생 가능 | ✅ invokeDynamic으로 동적 처리, 더 가벼움  |

---


## 🧪 실습 예제 모음: 람다 vs 익명 클래스

### ✅ 예제 1: 버튼 클릭 이벤트 처리
```java
// 익명 클래스
button.setOnClickListener(new OnClickListener() {
    @Override
    public void onClick(View v) {
        System.out.println("버튼 클릭 (익명 클래스)");
    }
});

// 람다 표현식
button.setOnClickListener(v -> System.out.println("버튼 클릭 (람다)"));
```



### ✅ 예제 2: Runnable 실행 비교
```java
// 익명 클래스
Runnable r1 = new Runnable() {
    @Override
    public void run() {
        System.out.println("익명 클래스 실행");
    }
};

// 람다
Runnable r2 = () -> System.out.println("람다 실행");

r1.run();
r2.run();
```


## ✅ 예제 3: this 키워드 비교
```java
public class Outer {
    private String name = "Outer";

    public void test() {
        Runnable anon = new Runnable() {
            private String name = "Anonymous";
            @Override
            public void run() {
                System.out.println("익명 클래스 this: " + this.name);
            }
        };

        Runnable lambda = () -> {
            System.out.println("람다 this: " + this.name);
        };

        anon.run();
        lambda.run();
    }
}
```

### 🔍 실행 결과 분석
```java
public class Outer {
    private String name = "Outer";

    public void test() {
        Runnable anon = new Runnable() {
            private String name = "Anonymous";
            @Override
            public void run() {
                System.out.println("익명 클래스 this: " + this.name);
            }
        };

        Runnable lambda = () -> {
            System.out.println("람다 this: " + this.name);
        };

        anon.run();
        lambda.run();
    }
}
```

### 🧠 핵심 개념: this의 의미

| 컨텍스트         | this가 가리키는 대상           |
|------------------|-------------------------------|
| 익명 클래스 내부 | 익명 클래스 인스턴스 (`new Runnable() {}`) |
| 람다 내부        | 외부 클래스 인스턴스 (`Outer`) |


### ✅ 실제 출력 결과
```java
익명 클래스 this: Anonymous
람다 this: Outer
```
- anon.run() → 익명 클래스 내부의 this.name → "Anonymous"
- lambda.run() → 람다 내부의 this.name → 외부 클래스 Outer의 "Outer"



## ✅ 예제 4: 상태 유지 비교
```java
Runnable anon = new Runnable() {
    int count = 0;
    @Override
    public void run() {
        count++;
        System.out.println("익명 클래스 count = " + count);
    }
};

Runnable lambda = () -> {
    // int count = 0; // 컴파일 오류: 람다는 상태를 가질 수 없음
    System.out.println("람다는 상태를 가질 수 없음");
};
```

## ✅ 예제 5: 캡처링 비교
```java
public void captureTest() {
    final int a = 10;
    int b = 20; // 사실상 final
    int c = 30;

    Runnable anon = new Runnable() {
        @Override
        public void run() {
            System.out.println(a);
            System.out.println(b);
            // System.out.println(c); // 오류
        }
    };

    Runnable lambda = () -> {
        System.out.println(a);
        System.out.println(b);
        // System.out.println(c); // 오류
    };

    c++; // 사실상 final 아님
}
```


## ✅ 예제 6: Comparator 정렬
```java
List<String> list = List.of("banana", "apple", "cherry");

// 익명 클래스
list.sort(new Comparator<String>() {
    @Override
    public int compare(String o1, String o2) {
        return o1.compareTo(o2);
    }
});

// 람다
list.sort((s1, s2) -> s1.compareTo(s2));
```


## ✅ 예제 7: Function 사용
```java
Function<String, Integer> anon = new Function<String, Integer>() {
    @Override
    public Integer apply(String s) {
        return s.length();
    }
};

Function<String, Integer> lambda = s -> s.length();
```


## ✅ 예제 8: Predicate 필터링
```java
Predicate<Integer> evenAnon = new Predicate<Integer>() {
    @Override
    public boolean test(Integer n) {
        return n % 2 == 0;
    }
};

Predicate<Integer> evenLambda = n -> n % 2 == 0;
```


## ✅ 예제 9: Consumer 출력
```java
Consumer<String> printAnon = new Consumer<String>() {
    @Override
    public void accept(String s) {
        System.out.println("익명: " + s);
    }
};

Consumer<String> printLambda = s -> System.out.println("람다: " + s);
```


## ✅ 예제 10: Supplier 생성
```java
Supplier<String> anon = new Supplier<String>() {
    @Override
    public String get() {
        return "익명 클래스";
    }
};

Supplier<String> lambda = () -> "람다 표현식";
```


## ✅ 예제 11: 스트림에서 람다 활용
```java
List<String> names = List.of("Kim", "Lee", "Park");

List<String> result = names.stream()
    .filter(n -> n.length() >= 3)
    .map(String::toUpperCase)
    .toList();
```


## ✅ 예제 12: 람다와 메서드 참조
```java
List<String> list = List.of("a", "b", "c");

// 람다
list.forEach(s -> System.out.println(s));

// 메서드 참조
list.forEach(System.out::println);
```
---

