# 함수형 인터페이스

자바의 함수형 인터페이스와 제네릭, 람다 표현식, 자바 기본 제공 인터페이스들에 대한 내용을 체계적으로 설명하고 있습니다.  
아래에 핵심 내용을 구조적으로 정리.

## 📘 자바 함수형 인터페이스 정리

## 1️⃣ 함수형 인터페이스란?
- 단 하나의 추상 메서드를 가진 인터페이스
- 람다 표현식과 함께 사용 가능
- @FunctionalInterface 애노테이션으로 명시

## 2️⃣ 타입별 함수형 인터페이스

### ✅ 타입별로 따로 정의한 경우
```java
interface StringFunction {
  String apply(String s);
}

interface NumberFunction {
  Integer apply(Integer s);
}
```
- 장점: 타입 안전성 확보
- 단점: 코드 중복, 재사용성 낮음

### ✅ Object 기반 통합
```java
interface ObjectFunction {
  Object apply(Object s);
}
```
- 장점: 하나의 인터페이스로 모든 타입 처리 가능
- 단점: 다운캐스팅 필요 → 타입 안전성 저하

##  3️⃣ 제네릭 도입
```java
interface GenericFunction<T, R> {
  R apply(T s);
}
```
- 장점: 타입 안전성과 재사용성 모두 확보
- 사용 예:
```java
GenericFunction<String, String> upper = s -> s.toUpperCase();
GenericFunction<Integer, Integer> square = n -> n * n;
```


### 4️⃣ 람다와 타겟 타입
- 람다 표현식은 타겟 타입에 따라 타입이 결정됨
- 동일한 시그니처라도 인터페이스 이름이 다르면 호환되지 않음
```java
FunctionA a = i -> "value = " + i;
FunctionB b = a; // ❌ 컴파일 에러
```


## 5️⃣ 자바 기본 제공 함수형 인터페이스

| 인터페이스           | 메서드 시그니처     | 입력     | 출력     | 대표 용도               | 예시 설명                     |
|----------------------|----------------------|----------|----------|--------------------------|-------------------------------|
| `Function<T, R>`       | R apply(T t)         | ✅       | ✅       | 데이터 변환, 필드 추출  | 문자열 길이 반환, 타입 변환 등 |
| `Consumer<T>`          | void accept(T t)     | ✅       | ❌       | 출력, 저장, 로그 기록   | 콘솔 출력, DB 저장 등         |
| `Supplier<T>`          | T get()              | ❌       | ✅       | 값 공급, 객체 생성      | 랜덤 값 생성, 지연 초기화     |
| `Runnable`             | void run()           | ❌       | ❌       | 스레드 실행, 단순 작업  | 멀티스레드 작업 정의          |


## 6️⃣ 특화 함수형 인터페이스
### ✅ Predicate
```java
interface Predicate<T> {
  boolean test(T t);
}
```
- 조건 검사, 필터링 용도
- Function<T, Boolean>으로도 가능하지만 의도 표현이 명확함

### ✅ Operator
- UnaryOperator<T>: Function<T, T> 상속 → 단항 연산
- BinaryOperator<T>: BiFunction<T, T, T> 상속 → 이항 연산

## 🧠 핵심 요약
- 제네릭을 도입하면 타입 안전성과 재사용성을 동시에 확보할 수 있음
- 자바는 java.util.function 패키지에 다양한 함수형 인터페이스를 기본 제공
- 람다는 타겟 타입에 의해 타입이 결정되며, 인터페이스 이름이 다르면 호환되지 않음
- 의도에 맞는 인터페이스 선택이 중요함: 예) 조건 검사 → Predicate 사용


---

# Predicate / UnaryOperator / BinaryOperator

Predicate, UnaryOperator, BinaryOperator를 자바 기본 함수형 인터페이스와 동일한 형식으로 정리.

## 🔧 특화 함수형 인터페이스 요약

| 인터페이스               | 메서드 시그니처           | 입력     | 출력     | 대표 용도                     | 예시 설명                             |
|--------------------------|----------------------------|----------|----------|--------------------------------|----------------------------------------|
| `Predicate<T>`             | boolean test(T t)          | ✅       | ✅ (boolean) | 조건 검사, 필터링             | 짝수 판별, 문자열 포함 여부 등         |
| `UnaryOperator<T>`         | T apply(T t)               | ✅       | ✅       | 단항 연산 (입력=출력)         | 제곱 계산, 문자열 대문자 변환 등       |
| `BinaryOperator<T>`        | T apply(T t1, T t2)        | ✅✅     | ✅       | 이항 연산 (입력=출력)         | 두 수의 합, 최대값 비교 등             |


### 📌 간단 요약
- `Predicate`: 조건을 검사하여 true/false 반환
- `UnaryOperator`: 하나의 값을 받아 같은 타입으로 변환
- `BinaryOperator`: 두 값을 받아 같은 타입으로 연산

---

# 예제 코드

아래는 자바에서 자주 사용하는 함수형 인터페이스 7종에 대한 간단한 샘플 예제 코드입니다.  
각 예제는 람다 표현식을 활용하며, 실무에서 자주 쓰이는 방식으로 구성했습니다.

## ✅ 1. Function<T, R>
```java
import java.util.function.Function;

public class FunctionExample {
    public static void main(String[] args) {
        Function<String, Integer> stringLength = s -> s.length();
        System.out.println(stringLength.apply("hello")); // 출력: 5
    }
}
```
- 입력: String
- 출력: Integer
- 용도: 변환, 추출

## ✅ 2. Consumer<T>
```java
import java.util.function.Consumer;

public class ConsumerExample {
    public static void main(String[] args) {
        Consumer<String> printer = s -> System.out.println("출력: " + s);
        printer.accept("hello"); // 출력: hello
    }
}
```
- 입력: String
- 출력: 없음
- 용도: 출력, 저장, 로그

## ✅ 3. Supplier<T>
```java
import java.util.function.Supplier;
import java.util.Random;

public class SupplierExample {
    public static void main(String[] args) {
        Supplier<Integer> randomSupplier = () -> new Random().nextInt(100);
        System.out.println("랜덤 값: " + randomSupplier.get());
    }
}
```
- 입력: 없음
- 출력: Integer
- 용도: 값 공급, 객체 생성

## ✅ 4. Runnable
```java
public class RunnableExample {
    public static void main(String[] args) {
        Runnable task = () -> System.out.println("작업 실행!");
        task.run(); // 출력: 작업 실행!
    }
}
```
- 입력: 없음
- 출력: 없음
- 용도: 스레드 작업, 단순 실행

## ✅ 5. UnaryOperator<T>
```java
import java.util.function.UnaryOperator;

public class UnaryOperatorExample {
    public static void main(String[] args) {
        UnaryOperator<String> toUpper = s -> s.toUpperCase();
        System.out.println(toUpper.apply("hello")); // 출력: HELLO
    }
}
```
- 입력: String
- 출력: String
- 용도: 단항 연산 (입력 = 출력)

## ✅ 6. BinaryOperator<T>
```java
import java.util.function.BinaryOperator;

public class BinaryOperatorExample {
    public static void main(String[] args) {
        BinaryOperator<Integer> add = (a, b) -> a + b;
        System.out.println(add.apply(3, 5)); // 출력: 8
    }
}
```
- 입력: Integer, Integer
- 출력: Integer
- 용도: 이항 연산 (입력 = 출력)

## ✅ 7. Predicate<T>
```java
import java.util.function.Predicate;

public class PredicateExample {
    public static void main(String[] args) {
        Predicate<Integer> isEven = n -> n % 2 == 0;
        System.out.println(isEven.test(4)); // 출력: true
    }
}
```
- 입력: Integer
- 출력: boolean
- 용도: 조건 검사, 필터링

---
